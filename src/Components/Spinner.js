import React from 'react';
import './Spinner.css'; // You can define your spinner styles in this CSS file

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
